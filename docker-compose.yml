version: '3.8'

services:
  pos:
    image: convenience-store-pos
    build: .
    container_name: convenience-store
    restart: unless-stopped
    ports:
      - "3008:3000"
      - "3009:3001"   # HTTPS，手机扫码专用
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
